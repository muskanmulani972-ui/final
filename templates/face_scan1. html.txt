<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Face Verification</title>

    <style>
        body {
            background: #001f3f;
            font-family: Arial, sans-serif;
            text-align: center;
            color: white;
            padding: 30px;
        }
        video {
            width: 60%;
            border: 4px solid #00aaff;
            border-radius: 20px;
            box-shadow: 0px 0px 20px #00aaff;
        }
        #status {
            margin-top: 15px;
            font-size: 22px;
            font-weight: bold;
        }
        button {
            background: #007bff;
            color: white;
            padding: 12px 25px;
            font-size: 18px;
            border: none;
            border-radius: 10px;
            margin-top: 20px;
            cursor: pointer;
            transition: 0.3s;
        }
        button:hover {
            background: #005fcc;
        }
    </style>
</head>

<body>

    <h1>Face Verification System</h1>
    <p>Position your face in front of the camera and blink once.</p>

    <video id="cam" autoplay></video>
    <div id="status"></div>

    <button onclick="startScan()">Start Scanning</button>

    <form id="form" method="POST" enctype="multipart/form-data" action="/face_verify">
        <input id="frame" name="frame" type="hidden">
    </form>

<script>
    let video = document.getElementById("cam");

    // Start Camera
    navigator.mediaDevices.getUserMedia({ video: true })
        .then(stream => video.srcObject = stream);

    function startScan() {
        let canvas = document.createElement("canvas");
        canvas.width = video.videoWidth;
        canvas.height = video.videoHeight;
        let ctx = canvas.getContext("2d");

        ctx.drawImage(video, 0, 0, canvas.width, canvas.height);

        canvas.toBlob(blob => {
            let file = new File([blob], "liveframe.bin");

            let dataTransfer = new DataTransfer();
            dataTransfer.items.add(file);

            document.getElementById("frame").files = dataTransfer.files;
            document.getElementById("form").submit();

            document.getElementById("status").innerHTML = 
                "⏳ Verifying face...";
        }, "image/jpeg");
    }
</script>

</body>
</html>
